# FWV Raura - Lokale Konfiguration für Raspberry Pi
# Kopiere diese Datei nach .env und passe die Werte an

# ============================================
# DATABASE
# ============================================
DB_USER=localuser
DB_PASSWORD=sicheres-passwort-hier
DB_NAME=localdb

# ============================================
# DRUCKER (Epson TM-m30III)
# ============================================
# IP-Adressen der Drucker im lokalen Netzwerk
# Leer lassen wenn kein Drucker vorhanden

# Bar-Drucker (für Getränke)
PRINTER_BAR_IP=192.168.1.100
PRINTER_BAR_PORT=9100

# Küchen-Drucker (für Essen)
PRINTER_KITCHEN_IP=192.168.1.101
PRINTER_KITCHEN_PORT=9100

# ============================================
# CLOUD SYNC (Optional)
# ============================================
# Aktiviert die Synchronisation mit der Cloud-Instanz
CLOUD_SYNC_ENABLED=false

# Cloud API URLs
CLOUD_ORDER_API_URL=https://order.fwv-raura.ch/api
CLOUD_INVENTORY_API_URL=https://inventar.fwv-raura.ch/api

# Sync-Key (vom Vorstand generiert)
CLOUD_SYNC_KEY=

# ============================================
# NETZWERK / DNS
# ============================================
# mDNS (Avahi) läuft automatisch - kasse.local, kitchen.local,
# inventar.local funktionieren sofort ohne Konfiguration.
#
# Captive Portal (optional, verhindert Mobile-Data-Switch):
#   1. PI_IP hier setzen
#   2. docker compose --profile captive-portal up -d
#   3. Am MikroTik hAP: DHCP Network → DNS Server = PI_IP
PI_IP=192.168.1.10

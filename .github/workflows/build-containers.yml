name: Build and Push Docker Containers

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: feuerwehrverein-raura/fwv-raura

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    outputs:
      new_release_published: ${{ steps.semantic.outputs.new_release_published }}
      new_release_version: ${{ steps.semantic.outputs.new_release_version }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install semantic-release
        run: npm install -g semantic-release @semantic-release/changelog @semantic-release/git

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run semantic-release
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx semantic-release --dry-run > release-output.txt 2>&1 || true
          cat release-output.txt
          if grep -q "Published release" release-output.txt; then
            VERSION=$(grep -oP 'Published release \K[0-9]+\.[0-9]+\.[0-9]+' release-output.txt)
            echo "new_release_published=true" >> $GITHUB_OUTPUT
            echo "new_release_version=$VERSION" >> $GITHUB_OUTPUT
          fi
          # Actually run semantic-release (not dry-run)
          npx semantic-release || true

      - name: Determine version
        id: version
        run: |
          # Get latest tag or use 0.0.0
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          VERSION=${LATEST_TAG#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Using version: $VERSION"

  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      backend-events: ${{ steps.filter.outputs.backend-events }}
      backend-members: ${{ steps.filter.outputs.backend-members }}
      backend-dispatch: ${{ steps.filter.outputs.backend-dispatch }}
      backend-accounting: ${{ steps.filter.outputs.backend-accounting }}
      frontend-website: ${{ steps.filter.outputs.frontend-website }}
      frontend-pdf-designer: ${{ steps.filter.outputs.frontend-pdf-designer }}
      wiki-docs: ${{ steps.filter.outputs.wiki-docs }}
      order-backend: ${{ steps.filter.outputs.order-backend }}
      order-frontend: ${{ steps.filter.outputs.order-frontend }}
      order-kitchen: ${{ steps.filter.outputs.order-kitchen }}
      order-register: ${{ steps.filter.outputs.order-register }}
      inventory-backend: ${{ steps.filter.outputs.inventory-backend }}
      inventory-frontend: ${{ steps.filter.outputs.inventory-frontend }}
      spam-manager: ${{ steps.filter.outputs.spam-manager }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend-events:
              - 'docker/backend-events/**'
            backend-members:
              - 'docker/backend-members/**'
            backend-dispatch:
              - 'docker/backend-dispatch/**'
            backend-accounting:
              - 'docker/backend-accounting/**'
            frontend-website:
              - 'docker/frontend-website/**'
            frontend-pdf-designer:
              - 'docker/frontend-pdf-designer/**'
            wiki-docs:
              - 'docker/wiki-docs/**'
            order-backend:
              - 'simple-order-system/backend/**'
            order-frontend:
              - 'simple-order-system/frontend/**'
            order-kitchen:
              - 'simple-order-system/kitchen-display/**'
            order-register:
              - 'simple-order-system/register-app/**'
            inventory-backend:
              - 'simple-inventory-system/backend/**'
            inventory-frontend:
              - 'simple-inventory-system/frontend/**'
            spam-manager:
              - 'docker/spam-manager/**'

  build-backend-events:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.backend-events == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/backend-events
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-events:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-events:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-events:${{ github.sha }}

  build-backend-members:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.backend-members == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/backend-members
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-members:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-members:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-members:${{ github.sha }}

  build-backend-dispatch:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.backend-dispatch == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/backend-dispatch
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-dispatch:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-dispatch:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-dispatch:${{ github.sha }}

  build-backend-accounting:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.backend-accounting == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/backend-accounting
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-accounting:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-accounting:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api-accounting:${{ github.sha }}

  build-frontend-website:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.frontend-website == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/frontend-website
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-frontend:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-frontend:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-frontend:${{ github.sha }}

  build-frontend-pdf-designer:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.frontend-pdf-designer == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/frontend-pdf-designer
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-pdf-designer:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-pdf-designer:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-pdf-designer:${{ github.sha }}

  build-wiki-docs:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.wiki-docs == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/wiki-docs
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-wiki:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-wiki:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-wiki:${{ github.sha }}

  # Order System
  build-order-backend:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.order-backend == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./simple-order-system/backend
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-backend:latest
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-backend:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-backend:${{ github.sha }}

  build-order-frontend:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.order-frontend == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./simple-order-system/frontend
          push: true
          no-cache: true
          build-args: |
            VITE_API_URL=https://order.fwv-raura.ch/api
            VITE_WS_URL=wss://order.fwv-raura.ch/ws
            VITE_AUTHENTIK_URL=https://auth.fwv-raura.ch
            VITE_AUTHENTIK_CLIENT_ID=order-system
          tags: |
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-frontend:latest
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-frontend:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-frontend:${{ github.sha }}

  build-order-kitchen:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.order-kitchen == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./simple-order-system/kitchen-display
          push: true
          no-cache: true
          build-args: |
            VITE_API_URL=https://order.fwv-raura.ch/api
            VITE_WS_URL=wss://order.fwv-raura.ch/ws
          tags: |
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-kitchen:latest
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-kitchen:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-kitchen:${{ github.sha }}

  build-order-register:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.order-register == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./simple-order-system/register-app
          push: true
          no-cache: true
          tags: |
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-register:latest
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-register:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/feuerwehrverein-raura/order-register:${{ github.sha }}

  # Inventory System
  build-inventory-backend:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.inventory-backend == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./simple-inventory-system/backend
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/feuerwehrverein-raura/inventory-backend:latest
            ${{ env.REGISTRY }}/feuerwehrverein-raura/inventory-backend:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/feuerwehrverein-raura/inventory-backend:${{ github.sha }}

  build-inventory-frontend:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.inventory-frontend == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./simple-inventory-system/frontend
          push: true
          no-cache: true
          build-args: |
            VITE_API_URL=https://inventar.fwv-raura.ch/api
            VITE_AUTHENTIK_URL=https://auth.fwv-raura.ch
            VITE_AUTHENTIK_CLIENT_ID=inventory-system
          tags: |
            ${{ env.REGISTRY }}/feuerwehrverein-raura/inventory-frontend:latest
            ${{ env.REGISTRY }}/feuerwehrverein-raura/inventory-frontend:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/feuerwehrverein-raura/inventory-frontend:${{ github.sha }}

  build-spam-manager:
    needs: [release, detect-changes]
    if: needs.detect-changes.outputs.spam-manager == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./docker/spam-manager
          push: true
          no-cache: true
          build-args: |
            VERSION=${{ needs.release.outputs.version }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-spam-manager:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-spam-manager:v${{ needs.release.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-spam-manager:${{ github.sha }}

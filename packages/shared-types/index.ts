// FWV Raura — geteilte Typen
//
// Wird inkrementell befuellt. Aktuell leer — App-Migrationen extrahieren ihre
// API-Modelle (Member, Event, Registration, ...) schrittweise hierher.
//
// Stable-Releases dieses Package werden nicht via npm publiziert — Konsumenten
// sind ueber `workspace:*` im Mono-Repo verlinkt.

export {};

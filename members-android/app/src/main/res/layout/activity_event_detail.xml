<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primary">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/primary"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:navigationIconTint="@color/on_primary"
            app:titleTextColor="@color/on_primary" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:id="@+id/eventTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="22sp"
                android:textStyle="bold"
                android:visibility="gone" />

            <TextView
                android:id="@+id/eventSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                android:visibility="gone" />

            <View
                android:id="@+id/topDivider"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginVertical="12dp"
                android:background="@color/divider" />

            <TableLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:stretchColumns="1">

                <TableRow android:layout_marginBottom="6dp">
                    <TextView android:text="Wann:" android:textStyle="bold" android:paddingEnd="12dp" android:textColor="@color/text_primary"/>
                    <TextView android:id="@+id/eventWann" android:textColor="@color/text_primary"/>
                </TableRow>
                <TableRow android:id="@+id/rowWo" android:layout_marginBottom="6dp">
                    <TextView android:text="Wo:" android:textStyle="bold" android:paddingEnd="12dp" android:textColor="@color/text_primary"/>
                    <TextView android:id="@+id/eventWo" android:textColor="@color/text_primary"/>
                </TableRow>
                <TableRow android:id="@+id/rowAnmeldung" android:layout_marginBottom="6dp">
                    <TextView android:text="Anmeldung:" android:textStyle="bold" android:paddingEnd="12dp" android:textColor="@color/text_primary"/>
                    <TextView android:id="@+id/eventAnmeldung" android:textColor="@color/text_primary"/>
                </TableRow>
                <TableRow android:id="@+id/rowDeadline" android:layout_marginBottom="6dp">
                    <TextView android:text="Anmeldeschluss:" android:textStyle="bold" android:paddingEnd="12dp" android:textColor="@color/error"/>
                    <TextView android:id="@+id/eventDeadline" android:textColor="@color/error" android:textStyle="bold"/>
                </TableRow>
                <TableRow android:id="@+id/rowMaxPart" android:layout_marginBottom="6dp">
                    <TextView android:text="Max. Personen:" android:textStyle="bold" android:paddingEnd="12dp" android:textColor="@color/text_primary"/>
                    <TextView android:id="@+id/eventMaxPart" android:textColor="@color/text_primary"/>
                </TableRow>
                <TableRow android:id="@+id/rowKosten" android:layout_marginBottom="6dp">
                    <TextView android:text="Kosten:" android:textStyle="bold" android:paddingEnd="12dp" android:textColor="@color/text_primary"/>
                    <TextView android:id="@+id/eventKosten" android:textColor="@color/text_primary"/>
                </TableRow>
            </TableLayout>

            <TextView
                android:id="@+id/eventDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:lineSpacingExtra="2dp" />

            <TextView
                android:id="@+id/shiftsHeader"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:layout_marginBottom="6dp"
                android:text="Helfer-Schichten"
                android:textColor="@color/text_primary"
                android:textSize="16sp"
                android:textStyle="bold"
                android:visibility="gone" />

            <LinearLayout
                android:id="@+id/shiftsList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRegister"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="24dp"
                android:text="Anmelden"
                app:cornerRadius="12dp" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
